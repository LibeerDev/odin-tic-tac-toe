const gameState = {
    currentWindow: 'startMenu',
    currentPlayer: 'X',
};

const menuStates = {
    startMenu: 'startMenu',
    choosePlayersMenu: 'choosePlayersMenu',
    playMenu: 'playMenu',
}

export { gameState }